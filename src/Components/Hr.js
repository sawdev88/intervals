import React from 'react';
import { View } from 'react-native';

const Hr = () => {
  return <View style={ styles.hr } />
}

const styles = {
  hr: {
    backgroundColor: '#fff',
    height: 1,
    marginBottom: 16
  }
}

export default Hr;
